# #{} insère un autre élèment que du texte dans un string

# renvoie la phrase entre " "
puts "On va compter le nombre d'heures de travail à THP"
# donne le resultat du calcul des heures dans une phrase
puts "Travail : #{10 * 5 * 11}"
# donne le résultat en minutes dans une phrase
puts "En minutes ça fait : #{10 * 5 * 11 * 60}"

# renvoie la phrase entre " "
puts "Et en secondes ?"

# renvoie le resultat du calcul des secondes
puts 10 * 5 * 11 * 60 * 60

# renvoie la phrase entre " "
puts "Est-ce que c'est vrai que 3 + 2 < 5 - 7 ?"

# retourne si la comparaison est fausse
puts 3 + 2 < 5 - 7

# renvoie un string qui demande le resultat d'un calcul et le resultat de ce calcul dans le même string
puts "Ça fait combien 3 + 2 ? #{3 + 2}"
# renvoie un string qui demande le resultat d'un calcul et le resultat de ce calcul dans le même string
puts "Ça fait combien 5 - 7 ? #{5 - 7}"

# renvoie la phrase entre " "
puts "Ok, c'est faux alors !"

# renvoie la phrase entre " "
puts "C'est drôle ça, faisons-en plus :"

# renvoie un string qui demande le resultat d'une comparaison et le resultat de celle ci dans le même string
puts "Est-ce que 5 est plus grand que -2 ? #{5 > -2}"
# renvoie un string qui demande le resultat d'une comparaison et le resultat de celle ci dans le même string
puts "Est-ce que 5 est supérieur ou égal à -2 ? #{5 >= -2}"
# renvoie un string qui demande le resultat d'une comparaison et le resultat de celle ci dans le même string
puts "Est-ce que 5 est inférieur ou égal à -2 ? #{5 <= -2}"